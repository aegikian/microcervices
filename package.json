{
  "name": "microfront",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "start" : "npm run start -w host & npm run start -w admin & npm run start -w shop"
  }
}
